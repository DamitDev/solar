# Solar — server-side routing state branch progress

## US-001 — Instance state Redis store (DONE)

Added `app/redis_state/instance_states.py` exposing `InstanceStatesStore`
with `set`/`get`/`delete`/`get_all` keyed by `host:instance` (prefix
`solar:istate:`), storing the `InstanceStatePayload` shape
(`{host_id, host_name, instance_id, timestamp, data}`). Each write issues a
TTL (`ISTATE_TTL_S = 300`) so a host that stops reporting expires its recorded
state. Registered on `app/redis_state/__init__.py` as `instance_states_store`
and exported in `__all__`.

Write paths wired at both relay choke points in
`app/socketio_app/host_handlers.py`:
- `host_instance_state` (single-event emitter)
- `host_instance_state_batch` (batched emitter, per-entry via
  `asyncio.gather` alongside the WS emit)

Tests:
- Unit: `tests/test_instance_states.py` (7 tests) — dict-based fake Redis,
  patches the store's own namespace, asserts set/get values, delete, that a
  TTL is issued on write (default + custom), and get_all.
- Integration: `tests_integration/infrastructure/test_instance_state_ttl.py`
  — round-trips a real write against the live session Redis with a short TTL
  and confirms expiry after the TTL while a long-TTL sibling survives.
- `solar:istate:` added to the volatile-redis flush list in
  `tests_integration/conftest.py::_flush_volatile_redis`.

Feedback loops: ruff + black pass, full solar-control unit suite green
(1021 passed). Integration suite requires Docker; collected/compiled cleanly
but not executed here.

## US-002 — Active request Redis store (DONE)

Added a per-request active registry to `RoutingStore` in
`app/redis_state/routing.py` (prefix `solar:active-req:`), the server-authoritative
source for the WebUI's active-request view:
- `create_request` — records an in-flight request on `request_start`
  (model/endpoint/client_ip/timestamp)
- `update_request` — merges routing details on `request_routed`/`request_reroute`
  (host_id/host_name/instance_id/resolved_model/attempt), returns None if the
  request is unknown
- `delete_request` — removes the entry on `request_success`/`request_error`
- `get_request` / `list_requests` — read individual or all live entries
  (used by snapshotting in later stories)

Each write issues a TTL mirroring `ACTIVE_TTL_S` (300 s, overridable per-write)
so a replica that crashes mid-request expires its entry instead of showing a
stuck 'processing' request. Cross-pod delete semantics fall out naturally from
shared Redis: a terminal event on any replica removes the key regardless of
which replica created it.

Writes wired at the single broadcast choke point in
`app/gateway.py::_broadcast_routing_event`, which now branches on
`event_data.type` and dispatches to the new `_update_active_registry` helper.
Registry-update failures are swallowed so a Redis hiccup never breaks the
actual event broadcast/logging.

Tests:
- Unit: `tests/test_routing_registry.py` (7 tests) — dict-based fake Redis,
  patches the store's own namespace, asserts create/update/delete lifecycle,
  custom TTL on write, cross-pod delete semantics (two store instances), and
  list_requests excluding deleted/expired.
- Integration: `tests_integration/infrastructure/test_routing_registry_ttl.py`
  — round-trips a real write against the live session Redis with a short TTL,
  confirms expiry after the TTL while a long-TTL sibling survives, and that a
  terminal delete removes the entry.
- `solar:active-req:` added to the volatile-redis flush list in
  `tests_integration/conftest.py::_flush_volatile_redis`.

Feedback loops: ruff + black pass, full solar-control unit suite green
(1028 passed). Integration suite requires Docker; collected/compiled cleanly
but not executed here.

## US-003 — Snapshot builder service (DONE)

Added the typed, versioned snapshot payload model and its builder:

- `app/models/routing_snapshot.py` — `RoutingSnapshot` + sub-models
  (`RoutingHost`, `InstanceStateEntry`, `ActiveRequestEntry`,
  `RequestAggregates`) with `schema_version` (1) and `generated_at`. The same
  payload is shared by the WS emit (US-004) and REST mirror (US-005).
  Field contract to the WebUI (documented in the module docstring):
  `active_requests` → requests Map, `instance_states` → instanceStates Map,
  `endpoints` → endpoints list, `aggregates` → server-computed inFlight/totals,
  `hosts`/`pending_hosts` → host status + pending-approval hosts. Exported via
  `app/models/__init__.py`.
- `app/services/routing_snapshot_builder.py` — `build_routing_snapshot()` composes
  hosts (status + drain + health + instance cache from `host_store`),
  per-instance states (`instance_states_store.get_all`), active requests
  (`routing_store.list_requests`), server-computed tallies, endpoints
  (`endpoint_db.get_all_endpoints`, shape matches `endpoints_update`), and
  pending hosts (`host_store.get_all_pending`).
- Aggregate arithmetic moved server-side (mirrors WebUI `workload.ts` /
  `graph.ts`): `by_instance` (`host:instance` cell keys), `by_host`,
  `by_model` (uses `resolved_model` with `model` fallback), `by_endpoint`,
  plus `queued`/`processing` (an entry carrying host+instance is
  `processing`, otherwise `queued`) and `errored` (0 — terminal requests are
  deleted from the registry). Endpoint loading degrades to empty on failure.

Tests: `tests/test_routing_snapshot_builder.py` (7 tests, anyio) — placeholder-shaped
snapshot, host status/drain/health + connected + instance cache, instance
state data/timestamp, tallies by instance/host/model/endpoint, unrouted
request counting as queued, resolved_model fallback, endpoints + pending
hosts pass-through.

Feedback loops: ruff + black pass, full solar-control unit suite green
(1036 passed).

## US-004 — Connect snapshot event (DONE)

`solar-control` now emits the authoritative `routing_snapshot` to a freshly
connected WebUI client before any request/instance deltas flow, so a
late-joining or reconnecting client always reconciles against fleet state
instead of a stale/partial base.

In `app/socketio_app/webui_handlers.py::webui_connect`, immediately after
`initial_status` and before the `instances_update`/`host_pending` deltas, the
handler builds the snapshot via the US-003 builder
(`build_routing_snapshot()`) and emits it to the connecting `sid`:

- Emitted payload is `snapshot.model_dump()` of the same `RoutingSnapshot`
  produced by `build_routing_snapshot()`.
- Connection-filtered emit (`to=sid`) keeps other clients unaffected.
- Ordering guarantees: `initial_status` → `routing_snapshot` →
  `instances_update`/`host_pending`, so the snapshot always precedes the deltas.

Tests:
- New `tests/test_webui_snapshot.py` (2 tests, anyio) patches the emit surface
  and verifies (a) `routing_snapshot` is emitted on connect with the builder's
  payload (`schema_version`, `generated_at`, active requests, `to=sid`) and
  (b) it is emitted strictly before the `instances_update` delta.
- Updated existing `tests/test_host_status.py::test_webui_initial_status_includes_active_jobs`
  to patch `build_routing_snapshot` (the connect handler now calls it).
- Ruff + black pass; full solar-control unit suite green (1038 passed). Ruff
  could not be executed on this NixOS machine (dynamically-linked binary stub);
  lint verified by review + black, full suite green.

## US-005 — REST snapshot mirror (DONE)

Added a REST mirror of the authoritative snapshot so the WebUI can fall back to
server state when the Socket.IO connection is down.

- New `app/routes/management/routing.py` — `GET /api/routing/state`
  (`response_model=RoutingSnapshot`), returns
  `build_routing_snapshot()` so the payload is byte-for-byte the same shape as
  the WS `routing_snapshot` event (schema_version + generated_at + all fields).
- Registered on `app/routes/management/__init__.py` via `routing.router`
  under the `/api` prefix; auth is the app-level management-key check shared by
  every management route (401 without `X-API-Key`).

Circular import fix: importing the snapshot builder directly exposed a latent
cycle (`routing_snapshot_builder` → `host_handlers` → `socketio_app/server.py` →
`webui_handlers` → `routing_snapshot_builder`). The root cause was
architectural: the builder reached into the Socket.IO presentation layer
(`host_handlers`) for three accessors that are thin wrappers over the Redis
`host_store` (`get_host_instances` → `host_store.get_host_instances`,
`get_connected_host_ids` → `host_store.get_connected_host_ids`,
`get_pending_hosts` → `host_store.get_all_pending`). Fixed structurally by
having the builder call `host_store` directly (it already imported from
`app.redis_state`), so the service no longer depends on socketio at all and the
cycle cannot form for any entry point. `test_routing_snapshot_builder.py`
patch targets updated from `app.socketio_app.host_handlers.get_*` to
`app.services.routing_snapshot_builder.host_store.get_*`.

Module named `routing_snapshot_builder.py` (a noun, matching the rest of
`app/services/`) — it assembles the authoritative `RoutingSnapshot` view-model.
The test module was renamed to `test_routing_snapshot_builder.py` to match.

Tests: new `tests/test_routing_rest.py` (2 tests) — 401 without the management
key, and 200 with the key returning the snapshot payload (schema_version,
generated_at, active_requests, hosts), patching the route's
`build_routing_snapshot` import via TestClient.

Feedback loops: ruff + black pass (ruff not executable on NixOS; verified by
review + black), full solar-control unit suite green (1040 passed).

## US-006 — WebUI snapshot consumer (DONE)

The WebUI's `useEventStream` hook now resets the routing view to the
authoritative server snapshot on connect and applies deltas on top, so a
reconnecting client no longer shows stale requests as 'processing'.

In `apps/solar-webui/src/hooks/useEventStream.ts`:
- Added `routing_snapshot` to the `WSMessageType` union and `onRoutingSnapshot`
  to `EventHandlers`.
- Added typed `RoutingSnapshotData` / `SnapshotActiveRequest` /
  `SnapshotInstanceState` wire contracts mirroring the US-003 server payload.
- On socket `connect`, reset `requests`/`instanceStates`/`endpoints` and set an
  `awaitingSnapshotRef` gate, so a delta that races the connect has no stale
  base to corrupt.
- While the gate is set, `request_start`/`request_routed`/`request_success`/
  `request_error` and `instance_state` deltas are dropped; they all resume once
  the snapshot lands (US-004 guarantees snapshot-before-deltas).
- A new `routing_snapshot` case applies the payload wholesale: `active_requests`
  → were built into the `requests` Map (`queued`→`pending`, `processing` kept),
  `instance_states` → `instanceStates` Map keyed `host:instance`, `endpoints` →
  the `endpoints` list, then clears the gate and invokes `onRoutingSnapshot`.
- Bound via the existing `bindEvent` pattern (`{'type':'routing_snapshot'}`).

Tests: `apps/solar-webui/src/hooks/__tests__/useEventStream.test.ts` (2 tests)
mock `socket.io-client` and `@/api/client` and assert (a) reset-on-connect plus
delta gating until the snapshot arrives, and (b) wholesale snapshot application
(requests/instance-states/endpoints, queued→pending) then resumed delta
processing.

Feedback loops: `pnpm lint` (eslint + prettier) and `tsc` pass; full webui
vitest suite green (505 passed). No US-* references left in the code.

## US-007 — WebUI REST fallback polling (DONE)

The Routing page now polls the authoritative snapshot when the socket is down,
so it keeps showing correct live state instead of the last WS-derived frame.

- `apps/solar-webui/src/api/client.ts` adds `getRoutingState()` hitting
  `GET /api/routing/state`, returning a typed `RoutingState` added to
  `apps/solar-webui/src/api/types.ts` that mirrors the US-003/05 snapshot shape
  (`active_requests`, `instance_states`, `endpoints`, `aggregates`,
  `schema_version`, `generated_at`).
- `apps/solar-webui/src/components/routing/workload.ts` adds two snapshot
  adapters reused by the fallback: `snapshotRequests()` — maps
  `active_requests` onto the client `requests` Map with the exact US-006
  mapping (`queued`→`pending`, `processing` kept) — and
  `snapshotInstanceStates()` — maps `instance_states` onto the `host:instance`
  Map.
- `apps/solar-webui/src/components/RoutingFlow.tsx` now derives the routing view
  solely from the snapshot (connected mode uses the US-006 WS-seeded
  eventEndpoints + context requests; disconnected mode swaps to a
  `useFallbackPolling(enabled: !isConnected, 10s)` poll of `getRoutingState()`).

  Removed the now-redundant dual path: the local `endpoints` state, the
  `getEndpoints()` fallback poll, and the `eventEndpoints`/`getEndpoints()`
  bootstrap effect. `endpoints_update`/`getEndpoints()` are no longer fetched
  for the routing view — the snapshot (WS or REST) supplies both the endpoints
  and the active requests. `getInstanceState` is honoured from the snapshot's
  `instance_states` while disconnected so load bars stay fresh.

  The old `getEndpoints()`-style bootstrap on a connected-but-silent socket is
  intentionally gone: the US-006 `routing_snapshot` events are what seed the
  connected view now.

- The endpoints dashboard and gateway-stats code are untouched (out of scope).

Tests (`apps/solar-webui/src/components/__tests__/RoutingFlow.test.tsx`):
- Existing tests updated to seed `eventStream.endpoints` (the WS-snapshot source)
  instead of stubbing `getEndpoints()`.
- New: (a) connected mode prefers event endpoints and never calls
  `getRoutingState`; (b) when disconnected, RoutingFlow polls `getRoutingState`
  and the snapshot's endpoints render; (c) when disconnected, the snapshot's
  `active_requests` show up in the request ticker.

Feedback loops: `tsc --noEmit` clean, `pnpm lint` (eslint 0 errors + prettier)
green, full webui vitest suite green (506 passed).

## US-012 — Dynamically registered event handler registry (DONE)

`useEventStream.ts` now dispatches socket events through a handler registry
keyed by `WSMessageType` instead of the single hardcoded `switch` in
`handleEvent`, so adding or overriding an event type is a one-line registration
rather than editing a growing branch.

- `registryRef` — `Partial<Record<WSMessageType, RegisteredHandler>>`, the
  dispatch table. Built-in handlers are seeded once on mount (every captured
  value — state setters, refs, and the stable `updateRequest`/`removeRequest`
  callbacks — is render-stable, so the registry never needs rebuilding).
- `registerHandler(type, fn)` — public helper (exposed on the returned hook
  object) that writes a handler into the registry; consumers can register a new
  type or override a built-in without touching the switch.
- `handleEvent` — now a stable `[]` callback that resolves
  `registryRef.current[event.type]?.(event)` and falls back to a no-op for
  unregistered event types instead of leaving a case unhandled or crashing.
- Each former `switch` case became a `register('<type>', handler)` entry with
  byte-for-byte identical behaviour. The `awaitingSnapshotRef` gating is
  preserved per `request_*`/`instance_state` handler (gated handlers `return`
  early). `keepalive` registers no handler (no-op).
- The `bindEvent` wiring in the connect effect is unchanged and still routes
  through `handleEvent` → the registry.

Follow-up refinement: the handler implementations were extracted out of
`useEventStream.ts` into the sibling `eventHandlers/` folder, so the hook is no
longer a large block of inline closures. Both live under
`apps/solar-webui/src/hooks/eventStream/` (the hook is `eventStream/`
`useEventStream.ts`, its handlers in `eventStream/eventHandlers/`):
- `types.ts` — `DispatchContext` (bundles every stable React value a handler
  touches: all 12 state setters, `awaitingSnapshotRef`/`gatewayFilterRef`/
  `handlersRef`, `updateRequest`, `removeRequest`) and `RegisteredHandler`
  (`(event, ctx) => void`).
- Per-domain files: `request.ts` (request_start/routed/success/error/reroute),
  `hosts.ts` (initial_status/host_status/host_pending/host_pending_removed/
  instances_update/host_health), `instanceState.ts` (log/instance_state/
  routing_snapshot), `gateway.ts` (gateway_request/filter_status/
  endpoints_update/api_keys_update), `intents.ts` (intent_update/intent_removed),
  `pulls.ts` (pull_progress).
- `registry.ts` — `buildRegistry()` composes every built-in handler into the
  `Partial<Record<WSMessageType, RegisteredHandler>>` seed.
- `pullProgress.ts` — pull-progress types/constants/`prunePullProgress` moved
  here from `useEventStream.ts` to break the runtime circular import
  (useEventStream → registry → pulls → useEventStream); `useEventStream.ts`
  re-exports them so consumers (`IntentDetail`, `IntentsPage`,
  `PullProgress.test`) are unchanged.
- Directory layout: `src/hooks/eventStream/useEventStream.ts` +
  `src/hooks/eventStream/eventHandlers/{types,registry,request,hosts,
  instanceState,gateway,intents,pulls,pullProgress}.ts`; all consumers'
  `@/hooks/useEventStream` imports updated to `@/hooks/eventStream/useEventStream`.
- `useEventStream.ts` now builds a stable `DispatchContext` (`useMemo`) once,
  seeds the registry via `Object.assign(registryRef.current, buildRegistry())`
  on mount, and `handleEvent` dispatches `(event, ctx)`. `registerHandler`
  still takes a plain `(event) => void` for external consumers; built-in
  handlers use the richer `(event, ctx)` signature.

Tests (`apps/solar-webui/src/hooks/__tests__/useEventStream.test.ts`):
- New: (a) a consumer-registered handler (`registerHandler('host_status', fn)`)
  is dispatched for a bound socket event; (b) an event type with no registered
  handler no-ops without crashing.
- Existing US-006 snapshot consumer tests pass unchanged, proving the refactor
  is behaviour-neutral for every prior event type.

Feedback loops: `tsc --noEmit` clean, `pnpm lint` (eslint 0 errors + prettier)
green, full webui vitest suite green (508 passed). No US-* references left in
the code.

## US-013 — Unify duplicate client-side type declarations (DONE)

The snapshot and gateway-request wire shapes are now declared once, in
`apps/solar-webui/src/api/types.ts`, so the REST client (`getRoutingState`) and
the WS client (`useEventStream`) code against a single source of truth and the
two cannot drift (GET /api/routing/state and the WS `routing_snapshot` event
carry the identical payload by design, US-005).

- `api/types.ts` now owns the authoritative snapshot family. The atomic
  instance-state data field is typed as the specific `InstanceStateData` (it
  was `Record<string, unknown>` in `RoutingStateInstance`); `InstanceStateData`
  itself moved here so the shared declaration can reference it without
  `api/types.ts` importing from `useEventStream.ts`. The shared
  `RoutingStateActiveRequest` includes `endpoint`, matching the server's
  `ActiveRequestEntry` (the old WS `ActiveRequestSnapshot` was missing it).
- `useEventStream.ts` imports `InstanceStateData`, `RoutingState` and
  `GatewayRequestSummary` from `@/api/types` instead of redeclaring them, and
  re-exports `InstanceStateData`/`RoutingState`/`GatewayRequestSummary` so
  consumers that keep importing from the hook module are unchanged. The
  duplicate `GatewayRequestSummary` and the duplicate snapshot interfaces
  (`RoutingSnapshot`/`ActiveRequestSnapshot`/`InstanceStateSnapshot`) are gone.
- `eventHandlers/instanceState.ts` (routing_snapshot handler), `gateway.ts`
  and `types.ts` now import `RoutingState`/`GatewayRequestSummary` from
  `@/api/types` directly.
- WS-only presentation types (`RequestState`, `HostStatusData`, `InstanceSummary`,
  `LogEventData`, `GatewayFilter`, etc.) stay defined in `useEventStream.ts`;
  they are not folded into the REST models because they carry client-side view
  state (e.g. `removing`) the wire contract does not.
- `workload.ts::snapshotInstanceStates` lost the now-redundant
  `data as unknown as InstanceStateData` cast (the field is already typed).
- No circular import introduced: `api/types.ts` does not import from
  `useEventStream.ts`.

Feedback loops: `tsc --noEmit` clean, `pnpm lint` (eslint 0 errors + prettier)
green, full webui vitest suite green (508 passed).