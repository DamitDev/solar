# S-028: Build `supernova-step-download-model` image

## Description

SuperNova training pipelines need a step image that resolves a model artifact through the Data Repository and pulls the model files directly from Harbor into the shared job workspace. The Data Repository is the metadata and resolve authority; Harbor stores blobs; the step image performs ORAS transfer using `harbor-oci-client`.

This image must not write into Solar Host's managed inference `MODELS_DIR` cache. It writes only to the ephemeral job workspace mounted by Solar Host.

## Goal

- Build a Docker image named `supernova-step-download-model` in the `supernova-steps` repo.
- Accept model source URI and output path arguments, for example `repo://iris-osl:v3` and `/workspace/models/base`.
- Resolve the model through Data Repository, using D-014 or model version detail data from D-008 as appropriate.
- Pull the returned `harbor_ref` directly from Harbor with `harbor-oci-client` / ORAS into the requested workspace path.
- Validate destination paths stay under `/workspace` and fail clearly for missing URI, missing artifact, auth failure, or ORAS pull failure.
- Provide a minimal README or usage example and a local smoke test command.

## Additional Notes

- Repo: `supernova-steps` at `/mnt/nvme/AI/damit-aiops/supernova-steps`
- Key files likely: new image directory, Dockerfile, Python entrypoint, dependency manifest, README.
- Depends on D-014 (URI resolution endpoint) and D-008 (model version detail endpoint).
- Consumed by S-023/S-027 job execution and future SuperNova training job templates.

### S-021 Workspace Reference

The `/mnt/nvme/AI/damit-aiops/training-platform-project/docs/specs/job-step-workspace.md` defines the full contract. Key points for this step:

- **Environment input:** `MODEL_URI` (e.g. `repo://IRIS-BERT-base:v1`), `MODEL_OUTPUT_DIR` (e.g. `/workspace/models/IRIS-BERT-base`), plus all workspace paths (Section 4.1) and infrastructure credentials (Section 4.2).
- **Resolution:** Resolve `MODEL_URI` via Data Repository `GET /api/resolve?uri=...` to get the `harbor_ref`.
- **Pull:** Use `harbor-oci-client` / ORAS to pull the artifact from Harbor into `MODEL_OUTPUT_DIR`.
- **Write to config:** After successful pull, update `job.json` → `steps.download_model` with `model_dir`, `source_uri`, `harbor_ref`, `digest`, `size_bytes`. Use atomic write (write to `.tmp`, rename) to avoid corrupting the shared state.
- **Path safety:** Validate that `MODEL_OUTPUT_DIR` is under `/workspace/models/`. Fail with clear error if not.
- **Exit code:** 0 on success, non-zero on any failure (missing URI, resolve failure, Harbor auth/pull error).
