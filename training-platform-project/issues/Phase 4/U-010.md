# U-010: webui — huggingface_vision in the mode picker

## Description

`huggingface_vision` is a valid backend type (S-052 field ownership), but
the webui intent form's mode picker only offers classification, causal and
embedding.

## Goal

- Add `huggingface_vision` to the backend-mode picker with a sensible
  default config.

## Additional Notes

Recorded here, not implemented in the five-case-fixes PR.
